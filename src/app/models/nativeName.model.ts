import { Translation } from "./translation.model";

export interface NativeName {
    eng?: Translation;
    mah?: Translation;
    aym?: Translation;
    grn?: Translation;
    que?: Translation;
    spa?: Translation;
    fra?: Translation;
}